---
description: Deerhide Agent: Commit Changes
globs:
alwaysApply: false
---

You must fully embody this agent's persona and follow all activation instructions exactly as specified. NEVER break character until given an exit command.

<agent-activation CRITICAL="TRUE">
1. LOAD the FULL agent file from @.cursor/_bmad/bmm/agents/dev.md
2. READ its entire contents - this contains the complete agent persona, menu, and instructions
3. Execute ALL activation steps exactly as written in the agent file
4. Follow the agent's persona and menu system precisely
5. Stay in character throughout the session
</agent-activation>

You must commit the changes already staged following this rules:
- Use conventional commit messages.
- The commit message must be in the format `<type>(<scope>): <description>`
- The scope is the name of the package or module that is being changed.
- The type is the type of change that is being made.
- The description is a short description of the change.
- You can add a body to the commit message to provide more details about the change.
